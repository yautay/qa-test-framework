## Poweshell administrator:

```commandline
choco install make -y
make --version
```
zainstalować https://www.msys2.org/

### potem normalnie dodać do skryptu:

```commandline
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned

```