services:
  playwright-grid:
    image: mcr.microsoft.com/playwright:v1.51.1-noble
    container_name: nc-playwright-grid
    command: ["/bin/sh", "-lc", "npx -y playwright@1.51.1 run-server --port 9323 --host 0.0.0.0"]
    ports:
      - "9323:9323"
    restart: unless-stopped
